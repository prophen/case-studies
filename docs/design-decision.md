# Approved direction: builder’s studio with notebook touches

The owner found the original quiet-blue Engineering Field Notes sketches too sterile, liked the generated warmer concept, and instructed us to proceed. This approval supersedes the visual restrictions in the original handoff, while preserving its content/evidence boundaries and reusable architecture.

Use warm ivory paper (`#f5f1e8`), charcoal ink (`#1c2029`), ink blue (`#315f88`), and restrained rust (`#98442e`). Use locally bundled Inter for interface and editorial copy and Kalam for a few margin notes. Preserve a strong headline, annotated notebook diagram, split featured story, thin rules, and generous spacing. Keep the case-study chapter rail on desktop and visible section links on mobile.

The approved concept is saved alongside this document as `design-reference.png`. Its interface and copy are illustrative. Implementation replaces invented dates and platform integrations with a clearly labeled conceptual workflow. The original screenshots remain under `sketches/approved/` for information architecture reference.

The notebook graphic is an accessible SVG diagram of build/test/learn. The workflow is semantic HTML, visibly labeled as an illustration, without inert product controls. Neither is presented as evidence from the real application.
